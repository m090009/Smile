<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorAccent">@color/smileYellow_Dark</item>
    </style>
    <style name="FaceTracker" parent="Theme.AppCompat.NoActionBar"></style>
    <style name="TwoWayViewBase">
        <item name="android:background">#999999</item>
    </style>
    <style name="TwoWayView" parent="TwoWayViewBase">
        <item name="android:orientation">vertical</item>
        <item name="android:scrollbars">vertical</item>
    </style>
    <style name="CardView.NowCard">
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">@dimen/cardview_default_padding</item>
        <item name="android:layout_marginBottom">@dimen/cardview_default_margin</item>
        <item name="android:layout_marginLeft">@dimen/cardview_default_margin</item>
        <item name="android:layout_marginRight">@dimen/cardview_default_margin</item>
        <item name="cardElevation">@dimen/cardview_default_elevation</item>
        <item name="cardCornerRadius">@dimen/cardview_default_radius</item>
        <item name="cardUseCompatPadding">true</item>
    </style>
</resources>
